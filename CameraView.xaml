<UserControl x:Class="Picky.CameraView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
             xmlns:local="clr-namespace:Picky"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Expander Width="auto" IsExpanded="True" VerticalAlignment="Top">
            <Expander.Header>
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*" />
                        <!-- Expander header content -->
                        <ColumnDefinition Width="Auto" />
                        <!-- Button -->
                    </Grid.ColumnDefinitions>
                    <!-- Left column for Expander header content -->
                    <TextBlock Grid.Column="0" Text="Camera" VerticalAlignment="Center" />
                    <!-- Right column for the button -->
                    <ui:Button Grid.Column="1" Icon="FullScreenMaximize24" Command="{Binding FullScreenCommand}" Margin="20 0 10 0" HorizontalAlignment="Right" VerticalAlignment="Center" />
                </Grid>
            </Expander.Header>
            <Expander.Content>
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="700" />
                        <ColumnDefinition Width="150" />
                    </Grid.ColumnDefinitions>
                    <Image Grid.Column="0" x:Name="FrameImage" HorizontalAlignment="Stretch"></Image>
                    <StackPanel Grid.Column="2">
                        <ToggleButton x:Name="mftb" Content="Manual Focus" IsChecked="{Binding IsManualFocus}" Command="{Binding AutoFocusCommand}" Margin="20 0 10 0" HorizontalAlignment="Stretch" VerticalAlignment="Center" />
                        <Slider IsEnabled="{Binding IsChecked, ElementName=mftb}" Thumb.DragCompleted="FocusDragCompleted"  Minimum="0" Maximum="600" HorizontalAlignment="Stretch" Margin="20 10 15 10" />
                        <Label Content="Zoom" Margin="20 0 0 0"/>
                        <Slider Thumb.DragCompleted="ZoomDragCompleted" Minimum="1" Maximum="10" HorizontalAlignment="Stretch" Margin="20 10 15 10" />
                    </StackPanel>
                </Grid>
                
            </Expander.Content>
        </Expander>
    </Grid>
</UserControl>
